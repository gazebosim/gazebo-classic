package gazebo.msgs;
import "header.proto";
import "color.proto";
import "fog.proto";
import "shadows.proto";
import "visual.proto";
import "pose.proto";

message Scene
{
  required Header header = 1;
  optional Color ambient = 2;
  optional Color background = 3;
  optional string sky_material = 4;
  optional Shadows shadows = 5;
  optional Fog fog = 6;

  repeated Visual visual = 7;
  repeated Pose pose = 8;
}
