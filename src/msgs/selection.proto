package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Selection
/// \brief A message for GUI selection data
/// \verbatim

import "header.proto";

message Selection
{
  required string name   = 2;
  optional bool selected = 3 [default = false];
}

/// \endverbatim
