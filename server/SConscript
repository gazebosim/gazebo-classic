#Import variables
Import('env staticObjs sharedObjs')

dirs = Split('physics rendering sensors controllers')# bindings')

for subdir in dirs:
  SConscript('%s/SConscript' % subdir)

sources = ['main.cc',
           'Global.cc',
           'Vector3.cc', 
           'Quatern.cc',
           'Pose3d.cc',
           'World.cc',
           'XMLConfig.cc',
           'Time.cc',
           'Entity.cc',
           'GazeboError.cc',
           'UpdateParams.cc',
           'GazeboMessage.cc',
           'Vector2.cc',
           'Model.cc']

staticObjs.append( env.StaticObject(sources) )
sharedObjs.append( env.SharedObject(sources) )
