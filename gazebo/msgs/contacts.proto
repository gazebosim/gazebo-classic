package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Contacts
/// \brief Contacts from collision detection


import "contact.proto";
import "robot_msgs/time.proto";

message Contacts
{
  repeated Contact contact       = 1;
  required robot_msgs.Time time  = 2;
}
