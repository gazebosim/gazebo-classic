package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface LogStatistics
/// \brief A message statiscs about a log playback


import "time.proto";

message LogStatistics
{
  required Time  sim_time   = 2;
  required bool  paused     = 3;
  required uint64 iteration = 4;
}
