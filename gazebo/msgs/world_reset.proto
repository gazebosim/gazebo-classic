package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface WorldReset 
/// \brief A message that controls how the world is reset
/// \verbatim

import "header.proto";

message WorldReset
{
  optional bool time         = 1;
  optional bool model_poses  = 2;
}

/// \endverbatim
