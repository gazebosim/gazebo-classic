package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface MeshUpdate
/// \brief A message containing mesh update data

import "mesh.proto";

message MeshUpdate
{
  required string parent_name  = 1;
  optional uint32 parent_id    = 2;
  required Mesh mesh = 3;
  optional bool deformable = 4;
}
