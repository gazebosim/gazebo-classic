package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface LogRecordControl
/// \brief A message that allows for control of log recording functions

message LogRecordControl
{
  optional bool start           = 1;
  optional bool stop            = 2;
  optional bool paused          = 3;
  optional string base_path     = 4;
  optional string encoding      = 5;
}
