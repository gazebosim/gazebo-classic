package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Packet 
/// \brief Message that encapsulates another message with a type description


import "robot_msgs/time.proto";

message Packet
{
  required robot_msgs.Time stamp   = 1;
  required string type             = 2;
  required bytes serialized_data   = 3;
}
